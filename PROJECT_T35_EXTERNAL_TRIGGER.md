# T35 P3 GPIO 串口与高速外触发自动测试项目基准

> 文档状态：项目实施基准（Baseline）  
> 版本：v0.3  
> 更新日期：2026-09-01  
> 目标平台：Efinix Trion T35F324 / Efinity 2021.2

## 1. 文档用途

本文档是后续硬件接线、FPGA 设计、仿真、编译、下载和自动化测试的统一依据。后续实现必须优先遵循本文档；若相机规格、示波器实测结果或用户新要求与本文档冲突，应先更新版本、接口决策和变更记录，再修改工程。

本文档区分三类信息：

- **已确认**：由用户、原理图/PCB 导出资料、Efinity 工程或实测结果直接确认。
- **已锁定**：后续实现必须遵循的接口或架构决策。
- **待确认**：缺少相机规格或实测结果，不能据此直接接线或判定验收通过。

## 2. 最终目标

使用 T35 板 P3 五针接口上的四路 GPIO 和 GND，构成不依赖 FX3、DDR3、MIPI 数据通路及板载 IS281GB 光耦通道的自动化外触发测试控制器：

1. `GPIO1_IO` 接收相机光耦 Trigger Out，目标是可靠捕获约 200 ns 的低有效脉冲。
2. `GPIO2_IO` 向相机 Trigger In 发送周期、脉宽、极性和使能状态可配置的外触发信号。
3. `GPIO3_IO` 作为 UART TX，由 FPGA 向串口工装发送数据。
4. `GPIO4_IO` 作为 UART RX，由 FPGA 接收串口工装的数据。
5. P3-5 `GND` 作为上述串口和触发信号的公共参考地。
6. PC 端脚本通过 UART 配置、执行并记录自动化测试；Efinity 编译和下载流程沉淀为可复用能力。

P5 `TRIG_IN`、P5 `TRIG_OUT`、U25 和 U26 不参与本项目的实际信号路径，仅保留为原板硬件背景。

## 3. 命名与方向约定

全文统一采用**板卡/FPGA 视角**：

| 当前网络 | FPGA 方向 | 相机视角 | 文档含义 |
|---|---|---|---|
| `GPIO1_IO` / `TRIG_IN` 功能 | 输入 | 相机 Trigger Out | 相机光耦输出向板卡发送低有效脉冲，板卡负责捕获和测量 |
| `GPIO2_IO` / `TRIG_OUT` 功能 | 输出 | 相机 Trigger In | 板卡产生触发并发送给相机 |
| `GPIO3_IO` / `UART_TX` | 输出 | USB-UART RX | FPGA 发出的串口数据 |
| `GPIO4_IO` / `UART_RX` | 输入 | USB-UART TX | FPGA 接收的串口数据 |

`TRIG_IN`、`TRIG_OUT` 在本项目逻辑和协议中表示**拟定功能名**，其物理引脚分别是 `GPIO1_IO`、`GPIO2_IO`，不是 P5 上的同名板级网络。涉及 P5 时必须明确写成“P5 `TRIG_IN`”或“P5 `TRIG_OUT`”，避免混淆。

## 4. 已锁定的 P3 接口映射

| 板级网络 | P3 位置 | T35 球位 / Efinity 端口 | FPGA 方向 | 锁定功能 |
|---|---:|---|---|---|
| `GPIO1_IO` | P3-1 | C13 / `MUX_GPIO1` / `GPIOT_RXN00` | 输入 | 接收相机光耦 Trigger Out，捕获约 200 ns 低有效脉冲 |
| `GPIO2_IO` | P3-2 | E15 / `MUX_GPIO2` / `GPIOT_RXN03` | 输出 | 向相机 Trigger In 发送外触发 |
| `GPIO3_IO` | P3-3 | E14 / `MUX_GPIO3` / `GPIOT_RXN04` | 输出 | UART TX |
| `GPIO4_IO` | P3-4 | D14 / `MUX_GPIO4` / `GPIOT_RXP04` | 输入 | UART RX |
| `GND` | P3-5 | GND | — | 串口与触发信号参考地 |

四路 GPIO 均属于 1.8 V I/O Bank，I/O 标准为 1.8 V LVCMOS，不是 3.3 V、5 V、12 V 或 24 V 容忍接口。任何超过 FPGA 允许范围的信号都必须先经过合适的高速电平转换、缓冲和保护电路。

## 5. P3 电气实现基线

### 5.1 GPIO1：低有效高速触发输入

当前接收路径为：

```text
相机内部光耦开集电极输出
    -> 外接 1.8 V 上拉电阻（阻值待确认）
    -> P3-1 / GPIO1_IO
    -> R89 100 Ω
    -> C13 / MUX_GPIO1
    -> FPGA 同步、下降沿捕获、测宽和计数逻辑

相机光耦输出侧参考地
    -> P3-5 / GND
```

已确认事项：

- 相机光耦输出按开集电极、低有效方式使用：光耦导通时把 `GPIO1_IO` 拉低；光耦释放时由外接上拉恢复到高电平。
- 原板 R104 是 `GPIO1_IO` 到 GND 的 10 kΩ下拉，现已拆除，避免与外接上拉形成分压。
- 外接上拉电源已锁定为 1.8 V，上拉阻值尚待记录。
- 板载 R89 100 Ω串联电阻保留。

FPGA 实现要求：

- 将 `MUX_GPIO1` 配置为纯输入，关闭输出使能，不启用内部上拉或下拉。
- 使用至少两级同步寄存器处理异步输入，再进行低有效下降沿检测、脉宽测量和事件计数。
- 内部统一转换为含义明确的有效高事件脉冲，寄存器同时报告外部物理极性为低有效。
- 优先使用 100 MHz 逻辑时钟，10 ns 为一个采样周期；理想的 200 ns 脉冲约覆盖 20 个时钟周期。

200 ns 能否可靠捕获不只取决于 FPGA 时钟。外接上拉电阻与线缆、连接器、相机光耦晶体管及 FPGA 输入电容共同形成 RC，上升沿可能明显慢于下降沿。必须在 FPGA 引脚附近用示波器确认低电平、高电平、脉宽、上升/下降时间和重复频率后，才能判定达到 200 ns 目标。

### 5.2 GPIO2：触发输出

当前发送路径为：

```text
FPGA 触发发生器
    -> E15 / MUX_GPIO2
    -> R90 100 Ω
    -> P3-2 / GPIO2_IO
    -> 相机 Trigger In 或外部高速驱动级
```

- `GPIO2_IO` 是 1.8 V LVCMOS 推挽输出，不等同于隔离开集电极输出。
- 原板 R103 是 `GPIO2_IO` 到 GND 的 10 kΩ下拉，保留并用于上电/高阻期间提供默认低状态。
- 上电、复位、PLL 未锁定、UART 未配置和触发停止期间，`GPIO2_IO` 必须保持低电平。
- 若相机 Trigger In 不接受 1.8 V 推挽信号，必须增加高速缓冲、开集电极驱动或电平转换；转换后仍须满足目标脉宽和边沿要求。
- 触发周期、有效脉宽、逻辑极性、单次输出和连续输出由 UART 配置。

### 5.3 GPIO3/4：UART

- `GPIO3_IO` 配置为 UART TX 输出，串口空闲态为高。板载路径包含 R91 100 Ω串联电阻和 R102 10 kΩ下拉。
- `GPIO4_IO` 配置为 UART RX 输入，板载路径包含 R92 100 Ω串联电阻和 R101 10 kΩ下拉。
- 串口参数基线为 115200 baud、8N1。
- USB-UART 必须原生支持 1.8 V，或通过合适的 1.8 V ↔ 3.3 V 电平转换器连接；TX/RX 交叉连接。
- UART RX 正常连接时由工装 TX 主动维持空闲高电平。若要求断开工装时 `GPIO4_IO` 仍保持空闲高，应评估拆除 R101 或增加经过计算的外部上拉，不能只依赖 FPGA 弱上拉与 10 kΩ下拉对抗。

### 5.4 GND、隔离与保护

- 相机光耦输出侧参考地、P3 GPIO 触发接口地、UART 工装地必须按工装接线连接到 P3-5 `GND`。
- 当前方案绕过板载 U25/U26 隔离链路，因此必须明确系统共地关系，并评估地电位差、地环路和噪声。
- 工装应提供过压、ESD、浪涌和错误接线保护，但保护器件的带宽和电容不能破坏 200 ns 输入波形。
- 未确认电压、输出类型和地参考前，不得将相机信号直接接入 P3 GPIO。

## 6. P5 光耦链路：弃用方案与硬件背景

原板 P5 光耦网络为：

```text
P5-5 / TRIG_IN -> R94 100 Ω -> U25 IS281GB -> TRIG_IN_IO -> D11 / optic_trigin
C11 / optic_trigout -> TRIG_OUT_IO -> R95 100 Ω -> U26 IS281GB -> P5-4 / TRIG_OUT
```

- U25 输出侧由 R93 10 kΩ上拉至 1.8 V，外部有效信号经过光耦后在 FPGA 侧表现为低有效。
- U26 对外表现为隔离开集电极式下拉输出。
- ISOCOM 官方 IS281 规格给出的输出上升和下降时间均为典型约 6 µs、最大 18 µs，无法承担本项目约 200 ns 输入验收，也不适合严格的亚微秒输出边沿。
- 本项目不再使用 P5 `TRIG_IN/TRIG_OUT`、U25/U26、D11 `optic_trigin` 或 C11 `optic_trigout` 作为实际触发路径。相关内容只用于解释原板设计和避免误接。

## 7. 当前工作区资产

### 7.1 FPGA 工程

路径：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/`

- 工程文件：`T35_FRAMEWORK.xml`
- 顶层模块：`SRC/T35_TOP.v`
- 器件：Trion `T35F324`
- Efinity 版本：`2021.2.323.2.18`
- 最近工程已生成 `.bit` 和 `.hex`，但仍包含 DDR3、MIPI、FX3、I2C、Flash、GPS、相机控制等大量原功能，尚未成为本项目专用设计。
- `MUX_GPIO1/2/3/4` 当前仍需按新映射修改方向和 RTL 连接；现有 bitstream 不代表本项目功能已经实现。
- 当前 SDC 尚未为新的 UART 和异步高速触发输入建立完整约束策略。
- 原工程存在锁存器、隐式网络、重复声明及未连接端口等告警，派生工程必须审计并清除与本项目有关的告警。

### 7.2 硬件资料

路径：`T35_V2.1-PRJ/T35_V2.1-PRJ/`

- 原始 Altium PCB/原理图资料及 KiCad 转换工程均已存在。
- 有效器件和连接信息主要位于 `T35_V2/markdown/` 下的器件 Markdown 与 KiCad 原理图中。
- 自动生成的 `T35_V2.md` 接口信息不完整，不能单独作为接线依据。

### 7.3 `general_uart` 的作用与内容

路径：`general_uart/general_uart/`

`general_uart` 是独立的 Verilog UART 收发参考 IP，目前没有列入 `T35_FRAMEWORK.xml`，不会自动参与现有工程综合或 bitstream 生成。它仅提供 UART 字节帧物理层起点，不是完整的自动化测试控制器。

- `rtl/tx_clk_gen.v`、`rtl/rx_clk_gen.v`：发送波特率和接收采样时钟使能。
- `rtl/uart_frame_tx.v`、`rtl/uart_frame_rx.v`：UART 帧发送和接收。
- `rtl/uart_frame_top.v`：TX/RX 字节级封装。
- `tb/`：发送与内部环回仿真；VCD 是已有波形产物，不等同于自动验收。

它不包含命令解析、寄存器映射、CRC、触发控制、统计或 PC 自动化脚本。集成前必须修复 `uart_frame_tx_tb.v` 中实例化名称与实际模块不一致的问题，并补充自校验断言、超时和明确的通过/失败返回码。默认参数是 50 MHz、9600 baud；本项目应按最终系统时钟配置为 115200 baud、8N1。

## 8. FPGA 目标架构

建议从现有完整工程派生专用最小工程：

```text
P3-4 / GPIO4_IO / UART_RX
            |
            v
      UART RX -> 命令解析 -> 配置/状态寄存器 -> UART TX -> P3-3 / GPIO3_IO
                                  |
                                  v
                         可配置触发发生器
                                  |
                                  v
                         P3-2 / GPIO2_IO -> 相机 Trigger In

相机光耦 Trigger Out
    -> 外接 1.8 V 上拉
    -> P3-1 / GPIO1_IO
    -> 两级同步/下降沿捕获/计数/测宽/状态

相机光耦输出侧参考地 + UART 工装地 -> P3-5 / GND
```

### 8.1 保留与移除

保留 T35F324、P3 四路 GPIO、可靠的系统时钟/PLL、修正后的 `general_uart`，以及必要的复位、同步器、输入统计和上电安全逻辑。不实例化 MIPI、图像处理、DDR3、FX3、无关的 CMOS/GPS/I2C/Flash、旧 GPIO 复用以及 P5 光耦触发逻辑。物理 PCB 器件保持不变；这里的“移除”仅指专用 FPGA 工程中的 RTL、IP、端口和约束。

### 8.2 输入捕获

- `GPIO1_IO` 至少经过两级同步器后再做下降沿检测和测宽。
- 记录输入事件计数、最近一次有效低脉宽、最近一次周期、过窄脉冲和超时状态。
- 若实测有效脉冲小于两个采样周期，应采用异步置位捕获、输入寄存器或更高频时钟。
- 为同步器第一级和后续路径建立明确的 CDC/时序约束，并按 Efinity 支持方式标记同步寄存器。

### 8.3 触发输出

- 用计数器生成周期和脉宽可配置的 `GPIO2_IO` 输出；100 MHz 下建议内部寄存器以 10 ns 为单位。
- 必须保证 `0 < pulse_width < period`；非法配置返回错误且不更新工作寄存器。
- 使用影子寄存器并在周期边界原子更新，避免毛刺或截断当前脉冲。
- 复位和停止状态固定输出低；寄存器明确报告内部逻辑极性和相机端物理极性。

## 9. UART 控制协议基线

第一版采用二进制帧：

```text
SOF(2) | VERSION(1) | CMD(1) | LENGTH(1) | PAYLOAD(N) | CRC16(2)
```

| 命令 | 功能 |
|---|---|
| `PING` | 返回固件版本、协议版本和板卡状态 |
| `SET_PERIOD` | 设置触发周期 |
| `SET_WIDTH` | 设置触发有效脉宽 |
| `SET_POLARITY` | 设置输入/输出逻辑极性 |
| `START` / `STOP` | 启停周期触发输出 |
| `PULSE_ONCE` | 产生单次触发脉冲 |
| `READ_STATUS` | 读取运行状态、错误标志和当前配置 |
| `READ_INPUT_STATS` | 读取输入计数、周期、脉宽、超时和过窄标志 |
| `CLEAR_STATS` | 清除统计计数和锁存错误 |
| `LOOPBACK_TEST` | 通过符合 1.8 V 电平要求的工装回接 GPIO2 与 GPIO1 |

## 10. 自动化测试范围

PC 端 Python 工具至少应完成：

1. 自动发现或显式选择串口，执行 `PING` 和版本校验。
2. 配置周期、脉宽、极性、模式和运行次数。
3. 读取并校验 FPGA 状态、输入计数、周期和脉宽。
4. 在高速回环工装下扫描多组参数，包括约 200 ns 低有效输入场景。
5. 记录时间、串口、固件版本、参数、期望值、实测值、误差和失败原因。
6. 输出机器可读 JSON/CSV 和简洁的人类可读报告。
7. 将 Efinity 编译/下载与功能测试分层，便于独立验证和重试。

AI 可以协助执行和分析，但最终通过/失败判定必须由确定性脚本完成。

## 11. 分阶段实施计划

### 阶段 A：电气与工装冻结

- 记录 GPIO1 外接 1.8 V 上拉的实际阻值，确认 R104 已拆除。
- 获取相机 Trigger In/Out 的完整电气、极性和时序规格。
- 确认 GPIO2 是否需要外部高速驱动或电平转换。
- 确认共地、ESD/过压保护和 P3 线序。
- 在 FPGA 引脚附近测量 GPIO1 的 200 ns 波形和 GPIO2 输出波形。

交付物：电气参数表、工装接线图、上拉/驱动方案和示波器测量结果。

### 阶段 B：最小 FPGA 工程

- 从原工程建立专用副本，保留器件、时钟和 P3 四路 GPIO。
- 配置 GPIO1 输入、GPIO2 输出、GPIO3 输出、GPIO4 输入。
- 加入 UART、命令协议、寄存器、触发发生器、输入捕获和状态统计。
- 移除或不实例化 DDR、MIPI、FX3、P5 光耦路径等无关逻辑。
- 更新 SDC，处理系统时钟、GPIO1 异步输入和同步器约束。

交付物：可综合工程、bitstream、编译日志和告警审计。

### 阶段 C：仿真

- 修复并增强 `general_uart` testbench，验证 115200 baud、8N1。
- 验证协议帧、CRC、超时、错误响应和寄存器读写。
- 验证输出周期/脉宽边界、原子更新、单次脉冲、停止与复位状态。
- 验证 GPIO1 低有效 200 ns、临界窄脉冲、连续脉冲、抖动和超时场景。
- 建立自校验断言和明确通过/失败返回码。

### 阶段 D：上板与自动化

- 空载确认 GPIO2 和 GPIO3 的上电、复位及空闲电平。
- 使用 1.8 V UART 工装验证 GPIO3 TX、GPIO4 RX。
- 用高速信号源或回环工装验证 GPIO1 捕获与 GPIO2 输出。
- 用示波器验证幅值、极性、周期、脉宽、边沿、传播延迟和抖动。
- 接入相机完成外触发测试并固化 Python 自动化流程。

## 12. 验收标准

- 全文及工程统一为 GPIO1输入、GPIO2输出、GPIO3输出、GPIO4输入，不存在反向或 P5 当前路径描述。
- Efinity 端口方向与上述映射一致，上电无方向冲突。
- R104 已拆除；GPIO1 外接上拉电源为 1.8 V，实际阻值已记录。
- FPGA 引脚附近实测 GPIO1 高电平不超过允许范围，低电平满足输入阈值，200 ns 低有效脉冲具有足够的高低电平保持时间和边沿裕量。
- 连续测试无漏计；具体次数、误差和漏计上限在相机规格确认后冻结。
- GPIO2 周期、脉宽、极性、启动、停止和单次输出可配置，参数更新无额外毛刺或脉冲截断。
- 示波器确认 GPIO2 或其外部驱动级在相机端的实际电平和物理极性符合规格。
- UART 在 115200 baud、8N1 下连续通信无帧错误，并能正确报告协议、长度、CRC 和超时错误。
- 高速回环时输出计数与输入计数一致，脉宽/周期测量误差在定义容差内。
- Efinity 全流程无 error；新增逻辑不得引入锁存器、隐式网络、未约束关键时钟或关键 CDC 告警。
- PC 自动化脚本输出可追溯的 JSON/CSV 测试结果。

## 13. 当前待确认事项

1. GPIO1 外接 1.8 V 上拉电阻的实际阻值。
2. 相机光耦 Trigger Out 的最大集电极电流、导通压降、关断时间、最小/典型脉宽和最大频率。
3. GPIO1 在实际线缆和上拉条件下的高低电平、上升/下降时间及 200 ns 波形。
4. 相机 Trigger In 的电压阈值、输入类型、有效极性和最小脉宽；GPIO2 是否需要驱动转换。
5. `TRIG_OUT` 所需周期、脉宽、分辨率、误差和抖动范围。
6. UART 工装的 1.8 V 兼容性，以及断线时 GPIO4 空闲态处理方式。
7. 自动化测试次数、容许误差和漏计上限。
8. LED 是显示每个脉冲还是显示“输出已启用/近期活动”；LED 不得直接加载高速触发信号。

## 14. 后续修改原则

- 不在唯一的原始完整工程上做不可逆裁剪；建立专用副本并保留原始 bitstream 和报告。
- 每次完成一个可验证增量：最小顶层 → GPIO 安全方向 → UART 环回 → GPIO2 输出 → GPIO1 输入 → 协议统计 → 自动化。
- 每个增量均需经过自校验仿真、Efinity 编译和对应硬件测量，不能只以“编译通过”作为完成标准。
- 任何映射、电压、上拉、极性、协议或验收容差变化都必须先更新本文档。

## 15. 参考资料索引

- 原始需求：`E:/新建文件夹/新建 DOCX 文档 (2).docx`
- Efinity 工程：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/T35_FRAMEWORK.xml`
- FPGA 顶层：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/SRC/T35_TOP.v`
- GPIO 复用：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/SRC/MuxGpio_SignalSwitch.v`
- 引脚配置：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/T35_FRAMEWORK.peri.xml`
- 编译引脚报告：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/outflow/T35_FRAMEWORK.pinout.csv`
- 时序约束：`T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3_241225/T35_FRAMEWORK_V3/SRC/T35TOP_SDC.sdc`
- P3/P5 连接器导出：`T35_V2.1-PRJ/T35_V2.1-PRJ/T35_V2/markdown/1_25mm5pin_5_0e789b7a.md`
- GPIO 电阻连接导出：`T35_V2.1-PRJ/T35_V2.1-PRJ/T35_V2/markdown/resistor_2pin_1_2.md`
- 光耦连接导出：`T35_V2.1-PRJ/T35_V2.1-PRJ/T35_V2/markdown/is281gb_1_2_3_4.md`
- T35 芯片网络导出：`T35_V2.1-PRJ/T35_V2.1-PRJ/T35_V2/markdown/t35f324_324_ab3543b7.md`
- UART 参考代码：`general_uart/general_uart/`
- IS281/IS281GB 官方规格书：[ISOCOM IS281](https://isocom.com/wp-content/uploads/2018/06/dd93223-is281-310518.pdf)

## 16. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.3 | 2026-09-01 | 全部功能改用 P3：GPIO1=低有效高速触发输入、GPIO2=触发输出、GPIO3=UART TX、GPIO4=UART RX、P3-5=公共 GND；记录 R104 已拆除和 GPIO1 外接 1.8 V 上拉；P5/U25/U26 降级为弃用背景；更新架构、电气约束、实施阶段和验收标准 |
| v0.2 | 2026-08-31 | 曾锁定 GPIO3/4 UART 与 P5 光耦触发映射；补充 U25/U26、`general_uart` 和 200 ns 高速输入限制 |
| v0.1 | 2026-08-28 | 首次建立项目基准；整理 Word 需求、硬件资料、项目架构和阶段计划 |
